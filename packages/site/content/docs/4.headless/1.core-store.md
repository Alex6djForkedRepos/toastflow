---
title: Core Store
description: Use toastflow-core directly when you want full rendering control.
navigation:
  icon: i-tabler-automation
---

# Headless Core Store

Use `toastflow-core` when you want Toastflow behavior without the Vue UI.

::callout{icon="i-tabler-info-circle"}
If you are already in Vue or Nuxt and only want custom markup, use the [Headless Slot](/docs/headless/headless-slot) first. It is less work.
::

## Install

::code-group

```bash [pnpm]
pnpm add toastflow-core
```

```bash [npm]
npm install toastflow-core
```

```bash [yarn]
yarn add toastflow-core
```

::

## Create A Store

```ts [toast-store.ts]
import { createToastStore } from "toastflow-core";

export const store = createToastStore({
  position: "bottom-right",
  maxVisible: 3,
  queue: true,
});
```

## Render Your Own UI

::code-group

```ts [toast-store.ts]
import { createToastStore } from "toastflow-core";

export const store = createToastStore();
```

```ts [renderer.ts]
import { store } from "./toast-store";

store.subscribe((state) => {
  renderToasts(state.toasts);
  renderQueueCount(state.queue.length);
});
```

```ts [actions.ts]
import { store } from "./toast-store";

const id = store.show({
  type: "info",
  title: "Renderer connected",
});

store.update(id, {
  type: "success",
  title: "Updated",
});
```

::

## What You Keep

::card-group
::card{title="Queue behavior" icon="i-tabler-list-details"}
Keep overflow handling, max visible limits, and queue pause/resume.
::

::card{title="Timers" icon="i-tabler-stopwatch"}
Keep duration, pause-on-hover-compatible timer state, and loading updates.
::

::card{title="Events" icon="i-tabler-radio"}
Subscribe to state and Toastflow events for analytics or renderer updates.
::
::

## When To Use Core

::accordion
::accordion-item{label="Use core"}
Use it for non-Vue apps, design-system renderers, or advanced UI shells where you want full DOM control.
::

::accordion-item{label="Do not use core yet"}
If you are in Vue and only dislike the default card, use the headless slot. You keep accessibility helpers and avoid rebuilding renderer logic.
::
::
